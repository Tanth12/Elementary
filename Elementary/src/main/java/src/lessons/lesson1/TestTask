Тестовое задание №1

Имитация программы для проектирования квартир

Задача - в пакете src.lessons.lesson1 создать класс Room, включающий в себя высоту, ширину и длинну
Создать 3 приватные переменные типа int с именами:
height    (высота),
width     (ширина),
longitude (длинна)
Создать переменную типа String с именем name

Создать 2 публичных конструктора - один по-умолчанию, один - принимающий в себя все 3 поля

Создать (или сгенерировать) геттеры и сеттеры для всех полей

Создать публичные методы:
метод getSquare, возвращающий площадь комнаты в int
метод getPerimeter, возвращающий периметр комнаты в int
метод getSpace, возвращающий объем комнаты в int

Переопределить метод toString, чтобы он выводил строку вида:
"Room.name=*название комнаты*;Room.square=*площадь*;Room.perimeter=*периметр*"
все 3 параметра должны выводиться без пробела, разделение через точку с запятой, к примеру
"Room.name=kitchen;Room.square=20;Room.perimeter=16"
